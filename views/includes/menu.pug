.list-group
    if 5 > 0
        each category in categories 
            a(href=`/categories/${category.id}` class=category.id==selectedCategoryId?"list-group-item active":"list-group-item") #{category.name}
