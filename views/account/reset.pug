extends ../layouts/main-layout.pug

block content
    form(action="/login" method="POST")
        include ../includes/csrf.pug
        .label(for="email") Email:
        br
        input(type="text" id="email" name="email")
        br
        input.btn.btn-success(type="submit" value="Gönder")